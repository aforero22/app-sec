name = "cse-homework-worker"
main = "src/index.js"
compatibility_date = "2025-04-01"

# Route: apply this worker to the tunnel subdomain
# Configured after deployment via: wrangler deploy
# routes = [{ pattern = "tunnel.ossfia.ai/*", zone_name = "ossfia.ai" }]
